# MySQL persistence

Run `schema.sql` in MySQL to create the `smart_retail.detection_events` table.

The local demo continues to use CSV so it can run without database credentials. For production, configure the MySQL environment variables and use this schema as the persistent event store.
