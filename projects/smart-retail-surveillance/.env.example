# Optional path to a detection-event CSV.
# Relative paths are resolved from the smart-retail-surveillance project directory.
RETAIL_EVENTS=data/sample_detections.csv

# Flask API port
PORT=5000

# React frontend API URL
VITE_API_URL=http://localhost:5000
