# Model artifacts

Trained model files are intentionally not committed to this repository. They can be generated by running `src/train_transfer.py` on PlantVillage in a GPU environment.

Expected artifacts:

- `plant_disease_mobilenetv2.keras`
- `plant_disease_labels.json`
- `training_metrics.json`

Only measured metrics should be copied into the project README after training completes.
